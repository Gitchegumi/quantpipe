[pytest]
markers =
    unit: fast deterministic tests (<5s suite)
    integration: cross-module tests (<30s suite)
    performance: extended runtime tests (<120s suite)
    flakiness: meta-tests that run test suites multiple times (very slow)
    slow: slow tests excluded from default runs
addopts = -q -m "not flakiness"