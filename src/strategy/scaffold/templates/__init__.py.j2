"""{{ strategy_class_name }} strategy module.

This module exports the {{ strategy_class_name }} strategy for use in backtesting.
Generated by scaffold command.

Example:
    from src.strategy.{{ strategy_name }} import {{ strategy_constant }}

    # Use the global instance
    signals = {{ strategy_constant }}.generate_signals(candles, parameters)

    # Or import the class directly
    from src.strategy.{{ strategy_name }} import {{ strategy_class_name }}
    strategy = {{ strategy_class_name }}()
"""

from src.strategy.{{ strategy_name }}.strategy import (
    {{ strategy_class_name }},
    {{ strategy_constant }},
)

__all__ = ["{{ strategy_class_name }}", "{{ strategy_constant }}"]
